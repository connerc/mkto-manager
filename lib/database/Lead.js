/**
 * TODO Finish this class and other /database/ segment handlers.
 * 
 * TODO Determine how to "stream" these API Records using the BaseMkto instance.
 * 
 * @param {*} ParentClass 
 */

const Spawn = (ParentClass = BaseItem) => {
    class Lead extends ParentClass {
        constructor(data) {

        }
    }
};
